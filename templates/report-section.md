## {SECTION_NAME} ({count})
<!-- Template: replace {SECTION_NAME} and {count} with actual values.
     {headers}: pipe-separated column headers (e.g., "Severity | Location | Issue | Fix").
     {rows}: one or more issue-row.md lines joined by newlines. -->

| {headers} |
|------|------|------|------|
{rows}
