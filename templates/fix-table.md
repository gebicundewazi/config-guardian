### {TIER_NAME} — {description}
<!-- Template: replace {TIER_NAME} and {description} with actual values.
     {rows}: one or more issue-row.md lines joined by newlines. -->

| Issue | Location | Fix Applied |
|-------|----------|-------------|
{rows}
